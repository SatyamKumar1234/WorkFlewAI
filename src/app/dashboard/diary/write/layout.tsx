
'use client';

// This file is intentionally left empty. 
// The route has been moved to /app/diary/write/layout.tsx to enable a full-screen layout.

export default function DeprecatedWriteDiaryLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
